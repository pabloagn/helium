#!/bin/sh
set -eu
{{- if .firefoxPrivateProfile }}
exec "$HOME/.local/bin/helium-firefox" {{ .firefoxPrivateProfile | quote }} "$@"
{{- else }}
echo "Firefox private profile is not configured in Chezmoi." >&2
exit 1
{{- end }}
